import type { Metadata } from "next";
import { BasindaContent } from "@/components/BasindaContent";
export const metadata: Metadata = { title: "Press", description: "Berkay Doğan in the press: publications, echoes, and milestones." };
export default function Page() { return <BasindaContent lang="en" />; }
