import type { Metadata } from "next";
import { BasindaContent } from "@/components/BasindaContent";
export const metadata: Metadata = { title: "Press — Berkay Doğan", description: "Berkay Doğan in the press: publications, echoes, and milestones." };
export default function Page() { return <BasindaContent lang="en" />; }
