import type { Metadata } from "next";
import { HomeContent } from "@/components/HomeContent";
export const metadata: Metadata = { title: "Berkay Doğan — Tasfiye", description: "To destroy is the only way to build. Welcome to a 161-page execution of existence." };
export default function Page() { return <HomeContent lang="en" />; }
